// Subscription card temporarily hidden while fulfillment workflow is being refined.
// Re-enable by restoring the original component from git history.
export default function SubscriptionCard() {
  return null;
}