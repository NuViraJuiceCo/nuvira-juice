{
  "name": "DeliveryWaitlist",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "description": "Contact email for waitlist notification"
    },
    "address": {
      "type": "string",
      "description": "Address they tried to deliver to"
    },
    "zip": {
      "type": "string",
      "description": "ZIP code of the out-of-area address"
    },
    "description": {
      "type": "string",
      "description": "Optional notes about this waitlist entry (up to 1000 characters)",
      "maxLength": 1000
    },
    "notified": {
      "type": "boolean",
      "default": false,
      "description": "Whether we've notified them that delivery is available"
    }
  },
  "required": [
    "email"
  ],
  "rls": {
    "create": {},
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}