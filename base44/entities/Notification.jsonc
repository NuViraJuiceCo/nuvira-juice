{
  "name": "Notification",
  "type": "object",
  "properties": {
    "customer_email": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "description": {
      "type": "string",
      "description": "Optional additional context or admin note for this notification (up to 1000 characters)",
      "maxLength": 1000
    },
    "type": {
      "type": "string",
      "enum": [
        "order_update",
        "promotion",
        "new_drop",
        "general"
      ],
      "default": "general"
    },
    "notification_subtype": {
      "type": "string",
      "description": "Granular type: order_confirmation, delivery_reminder, out_for_delivery, delivered, subscription_renewal, subscription_payment_success, subscription_payment_failed, promo, loyalty_credit, production_reminder, general"
    },
    "order_id": {
      "type": "string"
    },
    "is_read": {
      "type": "boolean",
      "default": false
    },
    "icon": {
      "type": "string"
    },
    "deep_link": {
      "type": "string",
      "description": "In-app route to navigate to when notification is tapped, e.g. /account/orders"
    },
    "idempotency_key": {
      "type": "string",
      "description": "Unique key to prevent duplicate notifications for the same event"
    }
  },
  "required": [
    "customer_email",
    "title",
    "message"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "data.customer_email": "{{user.email}}"
    },
    "update": {
      "data.customer_email": "{{user.email}}"
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}